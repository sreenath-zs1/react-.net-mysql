apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: frontend
  name: frontend-deployment
spec:
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:  
      - image: pradeep34946/frontend:v6
        name: frontend
        env: 
        - name: BACKEND_API
          value: my_app.devpracon.tech